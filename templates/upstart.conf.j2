description "bitcoind"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5

console log

setuid {{ bitcoind_user_name  }}
setgid {{ bitcoind_user_group }}

exec /usr/local/bin/bitcoind -conf="{{ bitcoind_conf_dir }}/bitcoind.conf" -datadir="{{ bitcoind_data_dir }}"
